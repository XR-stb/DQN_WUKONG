# ============================================================
# 📊 训练数据监控配置
# ============================================================

# --- 数据存储 ---
data:
  train_data_dir: 'train_data/data'  # 训练日志CSV存储目录

# --- 实时仪表板 ---
dashboard:
  refresh_interval: 5000             # 刷新间隔 (毫秒)

# --- 里程碑报告 ---
report:
  milestone_interval: 20             # 每N局打印一次里程碑报告
